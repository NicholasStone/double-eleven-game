namespace DogeProperty {
  export enum State {
    Dead,
    Forward,
  }

  export enum Buff {
    NONE,
    MORE_UPPER_VELOCITY,
    LESS_UPPER_VELOCITY,
    INVINCIBLE,
    MOER_GRAVITY,
    LESS_GRAVITY,
  }
}

export default DogeProperty
